# Release pipeline integration review

Date: 2026-09-12. Reviewer: Codex, AI-assisted engineering review under the site owner's explicit authorization to repair release-flow integration and deploy after verification. No independent human/code-security certification is claimed.

## Deliberate integration scope

- Adopt the versioned audit orchestration, exact public-file inventories, structural workflow contracts, immutable artifact reuse and build-bound release/rollback manifest checks from the pending deployment work.
- Retain the installed Nginx v3 policy and its privileged helper unchanged. Do not add the pending historical PDF redirect. The public verifier does not claim that separate redirect is present.
- The bootstrap source now verifies copied seeds and rejects unsafe nodes, as required by its boundary regression tests. Bootstrap is NOT executed against the initialized production server.
- Keep all controlled SMTP/inbox evidence requirements. The single authorized internal inquiry from the preceding preparation remains the only submitted test. Application PHP and SMTP settings are unchanged; server configuration is not rewritten.
- Correct Windows npm subprocess invocation without a shell and normalize only text files whose Git attributes require LF. Binary downloads and the protected catalog project are excluded.
- Update the collaboration gate to validate the actual pinned orchestration policy, not search for a obsolete inline command string. No control, threshold, language or mandatory phase is removed.

## Legacy baseline transition

The active production SHA was obtained from the server and its deployed file manifest verified. The legacy baseline branch now verifies every old seal against the actual HTML bytes in that trusted Git commit, computes its semantic projection, and requires an exact reviewed before/after semantic and mechanical transition for changed pages. Existing evidence-record hashes, timestamps, review declarations, scope equality, ancestor checks and protected baseline injection remain enforced.

The writer's explicit --review-baseline-ref option rejects candidate HEAD, non-full SHAs, non-ancestors, altered readiness status, incompatible legacy provenance and a legacy seal that disagrees with Git. The new negative tests cover forged seals, duplicate/unsafe paths, missing review, incorrect parent hashes and stale transitions. Full editorial verification against production SHA 1df9f815143055992dcdf32850206b080831069a passed after consolidation; no old debt exception was renewed.

## Additional native-menu regression

When a page arrived with #mainNav, history.replaceState alone did not necessarily reset CSS :target. Closing now performs a real fragment navigation to the menu opener before cleaning the URL, preserving campaign parameters. A real-browser test opens that initial fragment, presses Escape, and requires the menu to be hidden, ARIA state false, and the campaign parameter preserved.

## Evidence status at preparation

- Editorial snapshot and added legacy-transition tests: PASS (17 tests).
- Workflow structure/anti-bypass tests: PASS (21 tests).
- Audit policy checks: PASS (16 controls, 58 configured gate references).
- Release boundary Node tests: PASS (9); Linux-specific server ownership test not applicable on Windows and still required in Linux CI. Running its shell fixture under Git Bash cannot emulate Linux chmod ownership and is not accepted as passing evidence.
- Public header/parser fixtures: PASS (19 offline cases).
- Search, local-file, native fallback, language-context and fragment-close browser checks: PASS.
- Full release audit and Linux/Windows deterministic build: pending final candidate CI. This document does not assert completion of those checks or authorize ignoring a failure.

## Deployment decision boundary

Keep protected EDITORIAL_TRUSTED_BASE_SHA at the currently active server commit until a successful accepted deployment. Only bind RELEASE_APPROVED_SHA and inquiry evidence to the final unchanged candidate after reviewing its validation results. The workflow must reject a mismatched active baseline, changed artifact, missing authorization or failed postdeployment browser/public checks and attempt rollback on a pre-commit failure.

The earlier release-preparation.md describes the pre-integration blocker state. This record supersedes its proposal to stop for new scope authorization; that authorization was subsequently provided. Neither document claims the production deployment has happened yet.
