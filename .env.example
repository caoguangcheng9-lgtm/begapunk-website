# Copy these keys to the server-only .env file and fill in real values there.
# Never commit or upload the real .env file to GitHub.
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
SMTP_TO=sales@begapunk.com
